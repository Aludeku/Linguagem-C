#include <stdio.h>

int main()
{
/*  1)toda variavel possui um nome
    2)toda variavel possui um tipo
    3)toda variavel possui um tamanho
    4)toda variavel possui um valor

    a)é temporário
    b)quando um programa é fechado ou o pc é desligado, tudo que estiver na memória, não será salvo
    c)valores armazenados na memória são voláteis
    d)o valor antigo da nossa variavel, será sobreposto
*/
    printf("%d", i);

    return 0;
}
