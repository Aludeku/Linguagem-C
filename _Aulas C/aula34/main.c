#include <stdio.h>
#include <stdlib.h>

int main()
{
    //while (true)
    //do*while (true)
    //for

    ///for( parte1; parte2; parte3){
    ///parte1 -> onde declaramos a inicialização da variavel
    ///parte2 -> é a condição
    ///parte -> é onde incrementamos a variável
    ///}

    for (int i = 0; i<=10; ++i){
        printf("%i\n",i);
    }


    return 0;
}
